# Building  whonix-setup-wizard#

Install dependencies, build a package, and install:

    sudo apt-get install build-essential python-stdeb 
    ./build_deb.sh
    sudo dpkg -i deb_dist/whonix-setup-wizard*.deb

